# antlr4-sql

## Prerequisites

The following must be installed on your system:

- **Python 3.14+**
- **Java JDK 17 or newer** (required to run ANTLR)

Java is required only to generate the lexer/parser using the ANTLR JAR.
It is not required at runtime once the parser has been generated.
