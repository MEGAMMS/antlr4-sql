SELECT N'line1
line2' AS msg;
SELECT @sql += N'ALTER TABLE ' + QUOTENAME(s.name) + '.' + QUOTENAME(t.name) + N' DROP ' + QUOTENAME(c.name);
SELECT [dbo].[My]]Table] AS x;
SELECT "foo""Bar" FROM "sch""ema"."t";
CREATE TABLE #tmp(id INT); SELECT * FROM #tmp;
SELECT @@ROWCOUNT AS rc, @localVar AS lv;
/* outer /* inner */ */ SELECT 0xABCD\
EF, 0b1010\
01;
   SELECT 1; 
   GO
   SELECT 2;
